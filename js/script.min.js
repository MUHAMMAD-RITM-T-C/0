(function(){
  'use strict';

  /* ===================== i18n ===================== */
  var i18n = {
    fa: {
      status: 'سیستم آنلاین',
      opsTerminal: 'R.I.T.M // پایانه عملیات',
      tagline: 'خدمات فریلنس // ادیت و طراحی وب',
      heroDesc: 'خدمات ادیت ویدئو و برنامه‌نویسی وب با کیفیت بالا، پشتیبانی کامل و تحویل سریع. یکی از گزینه‌های زیر رو انتخاب کن و فرم پایین صفحه رو پر کن.',
      tgBtn: 'عضویت در کانال تلگرام ↗',
      baleBtn: 'عضویت در کانال بله ↗',
      sec1: 'خدمات ادیت ویدئو',
      card1Tag: 'پریمیر پرو',
      card1Title: 'ادیت با Adobe Premiere Pro',
      card1Desc: 'ادیت حرفه‌ای روی سیستم با نرم‌افزار پریمیر، کیفیت و دقت بالا.',
      price1: '۶۰,۰۰۰', price1u: 'تومان',
      card2Tag: 'ادیت موبایل',
      card2Title: 'ادیت با گوشی موبایل',
      card2Desc: 'ادیت سریع و آماده برای انتشار، مناسب کلیپ‌های شبکه‌های اجتماعی.',
      price2: '۱۰۰,۰۰۰', price2u: 'تومان',
      sec2: 'خدمات برنامه‌نویسی HTML',
      card3Tag: 'تک‌صفحه‌ای',
      card3Title: 'طراحی تک‌صفحه HTML',
      card3Desc: 'طراحی یک صفحه HTML اختصاصی به همراه آنلاین‌سازی کامل پروژه.',
      price3: '۲۵۰,۰۰۰', price3u: 'تومان',
      card4Tag: 'پروژه کامل',
      card4Title: 'پروژه کامل HTML + CSS + JS',
      card4Desc: 'طراحی کامل پروژه با جاوااسکریپت و استایل اختصاصی، به همراه آنلاین‌سازی.',
      price4: '۴۰۰,۰۰۰', price4u: 'تومان',
      note: '⏳ مدت زمان انجام هر پروژه با توجه به حجم، پیچیدگی و جزئیات سفارش متفاوت است و پس از بررسی درخواست، زمان دقیق تحویل اعلام می‌شود.',
      sec3: 'ارسال ویدیوی اختصاصی',
      videoDesc: 'اگر ویدیوی خاصی دارید که می‌خواید توسط ما ادیت بشه، می‌تونید مستقیم اون رو برای ما ارسال کنید.',
      videoBtnTg: 'ارسال به تلگرام',
      videoBtnBale: 'ارسال به بله',
      videoLabelTg: 'ارسال به تلگرام',
      videoLabelBale: 'ارسال به بله',
      copyBtn: 'کپی',
      copyBtnDone: 'کپی شد ✓',
      sec4: 'ثبت سفارش',
      labelName: 'نام شما',
      labelTelegram: 'آیدی تلگرام',
      labelService: 'نوع خدمت',
      phName: 'مثلاً: امیر',
      phTelegram: '@username',
      phService: 'یکی از گزینه‌های بالا رو انتخاب کن',
      opt1: 'ادیت با Adobe Premiere Pro — ۶۰,۰۰۰ تومان',
      opt2: 'ادیت با گوشی موبایل — ۱۰۰,۰۰۰ تومان',
      opt3: 'طراحی تک‌صفحه HTML — ۲۵۰,۰۰۰ تومان',
      opt4: 'پروژه کامل HTML+CSS+JS — ۴۰۰,۰۰۰ تومان',
      labelDetails: 'توضیحات پروژه',
      phDetails: 'جزئیات، رفرنس، مهلت تحویل مورد نظر و...',
      submitBtn: 'ارسال درخواست',
      submitBtnSending: 'در حال آماده‌سازی...',
      formOk: 'پیام سفارش کپی شد. صفحه تلگرام باز می‌شه — پیام رو همون‌جا paste و ارسال کن.',
      formErr: 'لطفاً همه فیلدهای ستاره‌دار رو پر کن.',
      footer: 'TheRITMCLAN'
    },
    en: {
      status: 'System online',
      opsTerminal: 'R.I.T.M // ops terminal',
      tagline: 'Freelance services // video editing & web dev',
      heroDesc: 'High-quality video editing and web development, full support and fast delivery. Pick an option below and fill out the form at the bottom of the page.',
      tgBtn: 'Join Telegram channel ↗',
      baleBtn: 'Join Bale channel ↗',
      sec1: 'Video editing services',
      card1Tag: 'Premiere Pro',
      card1Title: 'Editing with Adobe Premiere Pro',
      card1Desc: 'Professional desktop editing in Premiere Pro — high precision and quality.',
      price1: '60,000', price1u: 'toman',
      card2Tag: 'Mobile editing',
      card2Title: 'Editing on mobile',
      card2Desc: 'Fast, publish-ready edits — great for social media clips.',
      price2: '100,000', price2u: 'toman',
      sec2: 'HTML development services',
      card3Tag: 'Single page',
      card3Title: 'Single HTML page design',
      card3Desc: 'A custom single HTML page, fully deployed and put online for you.',
      price3: '250,000', price3u: 'toman',
      card4Tag: 'Full project',
      card4Title: 'Full HTML + CSS + JS project',
      card4Desc: 'A complete project with custom JavaScript and styling, deployed and put online.',
      price4: '400,000', price4u: 'toman',
      note: '⏳ Turnaround time depends on the size, complexity and details of each order. The exact delivery time is confirmed after we review your request.',
      sec3: 'Send us a specific video',
      videoDesc: 'Got a specific video you want us to edit? You can send it to us directly.',
      videoBtnTg: 'Send via Telegram',
      videoBtnBale: 'Send via Bale',
      videoLabelTg: 'Send via Telegram',
      videoLabelBale: 'Send via Bale',
      copyBtn: 'Copy',
      copyBtnDone: 'Copied ✓',
      sec4: 'Place an order',
      labelName: 'Your name',
      labelTelegram: 'Telegram handle',
      labelService: 'Service type',
      phName: 'e.g. Amir',
      phTelegram: '@username',
      phService: 'Choose one of the options above',
      opt1: 'Adobe Premiere Pro editing — 60,000 toman',
      opt2: 'Mobile editing — 100,000 toman',
      opt3: 'Single HTML page — 250,000 toman',
      opt4: 'Full HTML+CSS+JS project — 400,000 toman',
      labelDetails: 'Project details',
      phDetails: 'Details, references, your desired deadline, etc.',
      submitBtn: 'Send request',
      submitBtnSending: 'Preparing...',
      formOk: 'Your order message was copied. Telegram is opening — paste and send it there.',
      formErr: 'Please fill in all required fields.',
      footer: 'TheRITMCLAN'
    }
  };

  var STORAGE_KEY = 'ritm-lang';
  var currentLang = (function(){
    try{
      var saved = localStorage.getItem(STORAGE_KEY);
      if(saved === 'fa' || saved === 'en') return saved;
    }catch(e){}
    return document.documentElement.getAttribute('lang') === 'en' ? 'en' : 'fa';
  })();

  function applyLang(lang){
    var dict = i18n[lang];
    if(!dict) return;
    currentLang = lang;
    try{ localStorage.setItem(STORAGE_KEY, lang); }catch(e){}

    document.documentElement.setAttribute('lang', lang);
    document.documentElement.setAttribute('dir', lang === 'fa' ? 'rtl' : 'ltr');

    document.querySelectorAll('[data-i18n]').forEach(function(el){
      var key = el.getAttribute('data-i18n');
      if(dict[key] !== undefined) el.textContent = dict[key];
    });
    document.querySelectorAll('[data-i18n-placeholder]').forEach(function(el){
      var key = el.getAttribute('data-i18n-placeholder');
      if(dict[key] !== undefined) el.setAttribute('placeholder', dict[key]);
    });

    document.querySelectorAll('.lang-switch button').forEach(function(btn){
      btn.classList.toggle('active', btn.getAttribute('data-lang') === lang);
    });

    document.title = lang === 'fa'
      ? 'R.I.T.M | Freelance Ops'
      : 'R.I.T.M | Freelance Ops';

    tickClock();
  }

  document.querySelectorAll('.lang-switch button').forEach(function(btn){
    btn.addEventListener('click', function(){
      applyLang(btn.getAttribute('data-lang'));
    });
  });

  /* ===================== clock ===================== */
  var clockEl = document.getElementById('clock');
  function tickClock(){
    if(!clockEl) return;
    var now = new Date();
    var locale = currentLang === 'fa' ? 'fa-IR' : 'en-GB';
    clockEl.textContent = now.toLocaleTimeString(locale, { hour: '2-digit', minute: '2-digit', second: '2-digit' });
  }
  setInterval(tickClock, 1000);

  /* ===================== scroll reveal ===================== */
  var revealEls = document.querySelectorAll('.section.reveal');
  if('IntersectionObserver' in window){
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(entry){
        if(entry.isIntersecting){
          entry.target.classList.add('in-view');
          io.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
    revealEls.forEach(function(el){ io.observe(el); });
  } else {
    revealEls.forEach(function(el){ el.classList.add('in-view'); });
  }

  /* ===================== card selection ===================== */
  var serviceSelect = document.getElementById('serviceSelect');
  var allCards = document.querySelectorAll('.card[data-service]');

  function selectService(value, opts){
    opts = opts || {};
    allCards.forEach(function(card){
      card.classList.toggle('selected', card.getAttribute('data-service') === value);
    });
    if(serviceSelect && serviceSelect.value !== value){
      serviceSelect.value = value;
    }
    if(opts.scroll){
      var orderSection = document.getElementById('order');
      if(orderSection) orderSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
  }

  allCards.forEach(function(card){
    card.addEventListener('click', function(){
      selectService(card.getAttribute('data-service'));
    });
    card.setAttribute('tabindex', '0');
    card.setAttribute('role', 'button');
    card.addEventListener('keydown', function(e){
      if(e.key === 'Enter' || e.key === ' '){
        e.preventDefault();
        selectService(card.getAttribute('data-service'));
      }
    });
  });

  if(serviceSelect){
    serviceSelect.addEventListener('change', function(){
      selectService(serviceSelect.value);
    });
  }

  /* ===================== video send: glass bars ===================== */
  var videoBtnTg = document.getElementById('videoBtnTg');
  var videoBtnBale = document.getElementById('videoBtnBale');
  var glassBarTg = document.getElementById('glassBarTg');
  var glassBarBale = document.getElementById('glassBarBale');

  function toggleBar(bar, otherBar){
    if(!bar) return;
    var willOpen = !bar.classList.contains('open');
    if(otherBar) otherBar.classList.remove('open');
    bar.classList.toggle('open', willOpen);
    if(willOpen) bar.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }

  if(videoBtnTg) videoBtnTg.addEventListener('click', function(){ toggleBar(glassBarTg, glassBarBale); });
  if(videoBtnBale) videoBtnBale.addEventListener('click', function(){ toggleBar(glassBarBale, glassBarTg); });

  /* ===================== copy handle buttons ===================== */
  function copyText(text, btn){
    var dict = i18n[currentLang];
    function done(ok){
      if(!btn) return;
      var original = btn.textContent;
      btn.textContent = ok ? dict.copyBtnDone : original;
      btn.classList.toggle('copied', ok);
      setTimeout(function(){
        btn.textContent = dict.copyBtn;
        btn.classList.remove('copied');
      }, 1600);
    }
    if(navigator.clipboard && navigator.clipboard.writeText){
      navigator.clipboard.writeText(text).then(function(){ done(true); }, function(){ fallbackCopy(text, done); });
    } else {
      fallbackCopy(text, done);
    }
  }
  function fallbackCopy(text, cb){
    try{
      var ta = document.createElement('textarea');
      ta.value = text;
      ta.style.position = 'fixed';
      ta.style.opacity = '0';
      document.body.appendChild(ta);
      ta.select();
      document.execCommand('copy');
      document.body.removeChild(ta);
      cb(true);
    }catch(e){
      cb(false);
    }
  }

  document.querySelectorAll('.copy-btn').forEach(function(btn){
    btn.addEventListener('click', function(){
      var bar = btn.closest('.glass-bar');
      var handle = bar ? bar.querySelector('.handle') : null;
      if(handle) copyText(handle.textContent.trim(), btn);
    });
  });

  /* ===================== order form ===================== */
  var form = document.getElementById('orderForm');
  var statusMsg = document.getElementById('statusMsg');
  var submitBtn = document.getElementById('submitBtn');

  if(form){
    form.addEventListener('submit', function(e){
      e.preventDefault();
      var dict = i18n[currentLang];

      var name = form.elements['name'].value.trim();
      var telegram = form.elements['telegram'].value.trim();
      var service = form.elements['service'].value;
      var details = form.elements['details'].value.trim();

      if(!name || !telegram || !service || !details){
        statusMsg.textContent = dict.formErr;
        statusMsg.className = 'status-msg err';
        return;
      }

      var lines = currentLang === 'fa'
        ? ['سفارش جدید — R.I.T.M', 'نام: ' + name, 'تلگرام: ' + telegram, 'خدمت: ' + service, 'توضیحات: ' + details]
        : ['New order — R.I.T.M', 'Name: ' + name, 'Telegram: ' + telegram, 'Service: ' + service, 'Details: ' + details];
      var summary = lines.join('\n');

      if(submitBtn){
        submitBtn.disabled = true;
        submitBtn.textContent = dict.submitBtnSending;
      }

      copyText(summary, null);

      window.open('https://t.me/IamRITM', '_blank', 'noopener');

      statusMsg.textContent = dict.formOk;
      statusMsg.className = 'status-msg ok';

      if(submitBtn){
        setTimeout(function(){
          submitBtn.disabled = false;
          submitBtn.textContent = dict.submitBtn;
        }, 1200);
      }
    });
  }

  /* ===================== init ===================== */
  applyLang(currentLang);
})();
